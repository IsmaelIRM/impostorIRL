# Cloudflare Tunnel token for a STABLE URL on your own domain.
# Leave empty to use a quick tunnel (random *.trycloudflare.com URL, no setup needed).
# Get this token with:  cloudflared tunnel token <nombre-del-tunnel>
# See README "Exponerlo con Cloudflare Tunnel" for full steps.
CF_TOKEN=
